{
  "features": [
    {
      "id": "google-cloud-sdk",
      "name": "Install Google Cloud SDK",
      "options": {
        "version": {
          "type": "string",
          "default": "369.0.0-linux-x86_64",
          "description": "Select the version of the Google Cloud SDK to install."
        }
      },
      "include": ["ubuntu"]
    },
    {
      "id": "google-cloud-sql-auth-proxy",
      "name": "Install Google Cloud SQL Auth Proxy",
      "options": {
        "version": {
          "type": "string",
          "default": "latest",
          "description": "Select the version of the Google Cloud SQL Auth Proxy to install."
        }
      },
      "include": ["ubuntu"]
    }
  ]
}
